import routes_api = module("routes/api")
import db_db = module("db/db")

console.log('start:' + routes_api);
console.log('start:' + db_db);

/*
var Elements = module.exports = require('require-all')({
  dirname : __dirname,
  filter  : /([A-Z].+)\.js$/,
});
*/